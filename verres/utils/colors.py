import numpy as np

RED = np.array((255, 0, 0)).astype("uint8")
GREEN = np.array((0, 255, 0)).astype("uint8")
BLUE = np.array((0, 0, 255)).astype("uint8")
YELLOW = np.array((255, 255, 0)).astype("uint8")
CYAN = np.array((0, 255, 255)).astype("uint8")
PINK = np.array((0, 255, 0)).astype("uint8")
GREY = np.array((128, 128, 128)).astype("uint8")
WHITE = np.array((255, 255, 255)).astype("uint8")

COLOR = [RED, GREEN, BLUE, YELLOW, CYAN, PINK, GREY, WHITE]
